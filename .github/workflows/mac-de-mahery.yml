name: mac de Mahery

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Report
        run: |
          echo "Workflow: mac de Mahery"
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"
